{{ form_start(form) }}
    {# {{ form_widget(form) }} #}

      <div  class="row">
    <div class="col-md-6">
     
    {{ form_row(form.prenom,{ 'attr':{ 'placeholder':'prenom' } }) }}
     {{ form_row(form.nom) }}
      {{ form_row(form.dateNaissance) }}
       {{ form_row(form.tel) }} 
          {{ form_row(form.email) }}
       </div>
    <div class="col-md-6">
    
       {{ form_row(form.service) }}
        {{ form_row(form.specialites) }}
        </div>
    </div>
    <button class="btn btn-primary btn-block">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
